import { ZRef_ } from "./zref"

describe('basic tests', () => {

    it('can construct from let', async () => {
        //prepare
        //act
        //check
        expect(true).toStrictEqual(true)
    })

})